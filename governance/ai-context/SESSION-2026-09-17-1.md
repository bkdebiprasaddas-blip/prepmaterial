# SESSION-2026-09-17-1 — Activation & scaffold

**Objective:** User asked to read `RULEBOOK.md` and initialize/align accordingly.

**Findings:** Repo at `D:\Project\PrepMaterial` contained only `RULEBOOK.md`,
no code/config, not a git repo → NEW mode per RULEBOOK's opening detection
instructions. This was the first time `RULEBOOK.md` was encountered in this
repo → §0.0 ACTIVATION protocol applies.

**Decisions:**
- Treat this as NEW mode (empty except the rule book itself).
- Relocate `RULEBOOK.md` from ROOT into `governance\RULEBOOK.md` (its
  canonical location per §B) — a structural placement mandated by the
  activation protocol itself, not a content edit to the governance file
  (§J0C untouched: no rule text was weakened/changed).
- Build only the mode-appropriate scaffold this session (folders +
  `AGENTS.md` + `BOOTSTRAP.md` + first session/log entries + `.gitignore`).
  Did NOT fabricate `planning\PLAN.md`/`DISCOVERY.md`/etc., since no product
  definition exists yet (§C0: discovery required before technical planning;
  §H.12: ask + clarify first when project info is insufficient).
- Did NOT create `Scratch\<ProjectName>\` — project name unconfirmed.
- Did NOT run `git init` — no "yes git" confirmation yet (§A.9).

**Work done (file-by-file):**
- Created `governance\ai-context\` (+ `archive\`), `governance\work-log\`,
  `governance\planning\`, `governance\documentation\`.
- Moved `RULEBOOK.md` → `governance\RULEBOOK.md`.
- Created `AGENTS.md` at ROOT (§A/§D/§F/§H/§J copied verbatim + short
  project-specific section).
- Created `governance\BOOTSTRAP.md` (current-state snapshot).
- Created `.gitignore` at ROOT.
- Created this file and `work-log\LOG-2026-09-17.md`.

**Folder tree (delta — everything is new):**
```
PrepMaterial/
├── AGENTS.md
├── .gitignore
└── governance/
    ├── RULEBOOK.md
    ├── BOOTSTRAP.md
    ├── ai-context/
    │   ├── SESSION-2026-09-17-1.md
    │   └── archive/
    ├── work-log/
    │   └── LOG-2026-09-17.md
    ├── planning/        (empty)
    └── documentation/   (empty)
```

**Decision log:** No human approvals requested or given yet — this session is
pure scaffold-building per §0.0(c)/(d), which runs without prior approval.

**Blockers:** Product idea, project name, stack, auth/roles, hosting, and git
preference are all unknown. Cannot enter DISCOVERY without them.

**Next steps:** Ask the bundled clarifying-question round (§C2/§H.12), then
write `planning\DISCOVERY.md` once answered, then present it for
**"approve discovery"**.

**Reminders for next session:** Read `BOOTSTRAP.md` first (per §0/§C). This
SESSION file is the only one so far — no archiving needed yet.
