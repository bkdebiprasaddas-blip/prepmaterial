# SESSION-2026-09-17-2 — Planning + CODING for TYBCA Sem 5 Hub

**Objective:** User supplied a full 38-section product brief for a TYBCA Sem 5
exam-prep dashboard, said "code it" immediately.

**Deviation flagged + resolved:** "code it" was said before PLANNING/DESIGN/UI
gates were approved (§A.5/§H.2). Rather than stall, wrote the full planning
set in one pass (DISCOVERY, PLAN, ARCH-DESIGN, IMPL-SPEC, UI-SPEC, TODO,
SETUP-GUIDE, SECURITY-THREAT-MODEL, RELEASE-PLAN — all in
`governance\planning\` / `governance\documentation\`), presented it, then got
explicit approvals in sequence: "approve plan" → "approve design, UI is
final". All gates now satisfied per BOOTSTRAP.md.

**Inventory/inspection (STEP1/STEP2 of user's own §37 workflow):** 4 files
found in `Theroy Question Bank\`; 2 were duplicate Network Technologies
drafts — user chose the newer "Final Exam Study System" one. Inspected all 3
chosen files for: localStorage keys, external dependencies (none — fully
offline), header/nav structure, fixed-position CSS (to place the new nav
link without collision). Found and flagged a real bug: AWD's bookmarks/theme
were stored under NT-branded keys (`nt503_bookmarks`/`nt503_theme`) —
user approved renaming to `awd_bookmarks`/`awd_theme`.

**Work done (file-by-file):**
- `governance\planning\DISCOVERY.md`, `PLAN.md`, `ARCH-DESIGN.md`,
  `IMPL-SPEC.md`, `UI-SPEC.md`, `TODO.md` — created.
- `governance\documentation\SETUP-GUIDE.md` — created.
- `governance\planning\SECURITY-THREAT-MODEL.md`, `RELEASE-PLAN.md` — created
  (brief, since static/offline/no-backend — explicitly marked N/A where
  applicable rather than padded).
- `index.html`, `assets/css/app.css`, `assets/js/app.js`,
  `assets/js/subjects.config.js` — created (dashboard).
- `subjects/advanced-web-designing.html` — copied from
  `Theroy Question Bank\Advanced_Web_Designing_Master_Question_Bank.html`;
  nav snippet injected after `<body>`; `nt503_bookmarks`→`awd_bookmarks` and
  `nt503_theme`→`awd_theme` renamed (5 occurrences). Diff verified: only
  these lines changed.
- `subjects/php-mysql-wfs.html` — copied from
  `Theroy Question Bank\PHP_MYSQL_WFS_Master_Question_Bank_By_Marks_Modern_Question_Bank.html`;
  nav snippet only. Diff verified.
- `subjects/network-technologies.html` — copied from
  `Theroy Question Bank\Network_Technologies_503_Question_Bank_Final_Exam_Study_System.html`;
  nav snippet only. Diff verified.
- `README.md` — created.

**Verification performed:** `node --check` on both new JS files (pass);
confirmed every file `index.html`/subject pages reference actually exists at
the referenced relative path; `diff` of each subject file against its
original confirmed no unintended changes; `<script>`/`<style>` tag-balance
check on all 4 touched/new HTML files.

**Verification NOT performed (§J13 — stated explicitly, not implied):**
actual browser rendering/interaction — Chrome extension was not connected
this session. TODO.md TESTING phase (per-subject feature checks, responsive
breakpoints, console-error check) is still open.

**Decision log:** git init (explicit user request) → PLANNING written →
"approve plan" (user) → "approve design, UI is final" (user) → CODING
executed under the already-given "code it".

**Blockers:** none for further planning; TESTING needs either the user to
open `index.html` themselves and report back, or browser access in a future
session.

**Next steps:** Run through TODO.md's TESTING checklist (ideally in an
actual browser); stage the new files; ask user whether to commit.

**Reminders for next session:** Read BOOTSTRAP.md first. This is SESSION
file #2 of the day — no archiving needed yet (retention cap is 3).
