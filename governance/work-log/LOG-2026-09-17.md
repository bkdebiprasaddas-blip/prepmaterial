# LOG-2026-09-17 — Plain-English log

## Morning: scaffold setup
See earlier entry — set up the governance folders, AGENTS.md, .gitignore.

## Afternoon: built the TYBCA Sem 5 hub

**What you asked for:** a homepage that lists your 3 Sem 5 subjects as
clickable cards, each opening the existing study-system HTML file for that
subject, without touching their content or breaking any of their features.

**What I did:**
1. Looked at your 3 subject files closely first. Found that two of your 4
   files were actually the same subject (Network Technologies) — an older
   draft and a newer, much more complete version. You picked the newer one.
2. Found a real bug while inspecting: your Advanced Web Designing file was
   saving its bookmarks and theme choice under storage keys named after
   Network Technologies (`nt503_...`) — clearly left over from however it
   was originally built. You approved fixing it (renamed to `awd_...`).
3. You said "code it" right away, but your rule book requires the plan and
   design to be written up and approved first — so I wrote all of that up
   quickly (what files, what changes, what the site looks like) and you
   approved it in two messages.
4. Built the dashboard (`index.html` + styling + a small script) and copied
   your 3 subject files into a `subjects\` folder, making only two kinds of
   changes to them: a small "← Sem 5" button in the corner of each, and the
   one bookmark/theme key rename in the AWD file.
5. Double-checked my own work: confirmed the site's files all reference each
   other correctly, the JavaScript has no syntax errors, and — most
   importantly — compared each subject file against your original file
   line-by-line to prove nothing else changed.

**What I could NOT do:** actually click around in a real browser to test it,
because the Chrome browser connection wasn't available this session. So I
can't yet promise the dashboard *looks* right or that search/bookmarks/dark
mode still work when clicked — that still needs to be checked, either by you
opening `index.html` yourself, or in a future session with browser access.

**Current state:** Site is built at the project root (`index.html`,
`assets\`, `subjects\`, `README.md`). Git has these new files, all staged,
not committed.

## Evening: pushed to GitHub, then live-tested in your browser

**Push:** You asked to push. Nothing was committed yet, and git didn't know
who you were, so I asked you to set your git name/email (you did), I
committed, you gave me the GitHub URL
(`bkdebiprasaddas-blip/prepmaterial`), and I pushed. It's live there now.

**Testing:** You reconnected the Chrome extension, so I actually opened the
site and clicked through it instead of just reading the code.

Good news: the dashboard, dark mode, "Continue Studying," and PHP's search
all work exactly as intended, with no errors.

**Bad news I caught and fixed:** the little "← Sem 5" button in the corner
of the Advanced Web Designing and PHP pages was sitting right on top of
their titles ("Advanced Web Designing" was showing as "dvanced Web
Designing"). Network Technologies was fine. I fixed it by moving the button
to sit next to the title instead of floating over it, re-checked with a
screenshot, and confirmed it's now clean on all three.

**What's still not checked:** how the site looks on a phone-sized screen
(the tool I'd use for that didn't cooperate this session), and I didn't
click through every single feature (bookmarks, focus mode) on all 3 subjects
individually — just a representative one (PHP's search). Recommend you take
a look at those yourself when you get a chance.

## Later: phone testing + more color + a real bug fix

You checked the site on your phone (had to open a firewall port first,
which you did in an admin terminal) — confirmed working. Then asked for the
homepage to be more colourful, and I added a distinct accent color per
subject (blue/violet/green) plus a subtle gradient on the title.

Then you reported the "More options" (⋮) menu on the Advanced Web Designing
page wasn't working on mobile. I tracked it down to two separate bugs
stacked on top of each other in that page's own original CSS — nothing to
do with the integration, both pre-dated my changes. Fixed both: the menu
now actually opens and shows on screen instead of being invisible. Checked
PHP and MySQL's and Network Technologies' own "more" menus too, in case they
had the same problem — they don't, so I left those two files alone.

## Later still: full check-up + making the subjects match

You asked me to check for any issues anywhere and make the subject pages
look consistent with each other. I asked what you meant by "consistent"
since your original brief specifically said to keep each subject's own
look — you confirmed you want both: fix anything broken, AND actually
unify the visual style.

**Check-up:** went through all 4 pages (dashboard + 3 subjects) looking
for the same kind of hidden-menu bug I found earlier, tested search/
bookmarks/dark-mode on all three, and double-checked that each subject's
saved data (bookmarks, theme) really doesn't clash with the others in your
browser's storage. Nothing else broken was found.

**Consistency:** all three subject pages already color themselves using a
small set of reusable style "variables" at the top of each file, so I was
able to just change those to match the dashboard's blue/purple palette
without touching any of the actual features, layout, or questions. Network
Technologies already happened to match, so nothing changed there. Advanced
Web Designing and PHP/MySQL now share the same blues/purples, backgrounds,
and text colors as the dashboard, in both light and dark mode. Re-tested
everything afterward to make sure nothing broke — it didn't.

## Still later: NT's header needed real structural work, not just color

You were right that color alone wasn't enough — Network Technologies'
header was still a tall, boxed-in card while the other two are slim bars
across the top. Fixed that too: made NT's header span edge-to-edge like
the others (and caught a small bug of my own making along the way — the
quick way of doing that caused a few pixels of sideways scroll, which I
found and fixed before it went anywhere).

Then you asked for it to be compact like the other two, not just full-width
— so I trimmed it down: dropped the extra label/date badge and description
line, cut the stats row from 7 numbers down to 2 (Questions, Shown — same
as what Advanced Web Designing already shows), and now it's the same
height as the other two headers. Checked it still scrolls correctly, still
works in dark mode, and search/bookmarks still work.

**Current state:** everything through today (scaffold, site build, colors,
bug fixes, header consistency pass) is either already pushed to GitHub or
staged and ready — check with me if unsure, or just say "push" and I'll
handle it.
