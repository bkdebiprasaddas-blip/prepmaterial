# Disposable/ephemeral prep material — never tracked (RULEBOOK §B)
Scratch/

# Secrets — never tracked (RULEBOOK §A.9 / §J6)
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example

# Common dependency / build / local-state folders (added per stack once chosen)
node_modules/
dist/
build/
*.log
